import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tech-task-pokemon-ionic-react-gql',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
